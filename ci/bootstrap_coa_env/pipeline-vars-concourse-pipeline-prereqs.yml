# ###################
# This file lists required keys. These keys must be present, but values are ignored in CI environments.
# ###################
pipeline_vars:
  coa-config-key: value-from-coa-config
